{
  "name": "language-learning-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run -w packages/core build && npm run build --workspace=apps/web",
    "build:vercel": "npm run -w packages/core build && cd apps/web && npm install && npx prisma generate && npm run build",
    "start": "npm run start --workspace=apps/web",
    "worker": "npm run dev --workspace=apps/worker",
    "db:generate": "cd apps/web && npx prisma generate",
    "db:push": "cd apps/web && npx prisma db push",
    "db:migrate": "cd apps/web && npx prisma migrate dev"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
