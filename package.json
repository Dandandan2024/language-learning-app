{
  "name": "language-learning-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "cd packages/core && npm run build && cd ../.. && cd apps/web && npm run build",
    "build:vercel": "cd packages/core && npm install && npm run build && cd ../../apps/web && npm install && npm run build",
    "start": "npm run start --workspace=apps/web",
    "worker": "npm run dev --workspace=apps/worker",
    "db:generate": "cd apps/web && npx prisma generate",
    "db:push": "cd apps/web && npx prisma db push",
    "db:migrate": "cd apps/web && npx prisma migrate dev"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
